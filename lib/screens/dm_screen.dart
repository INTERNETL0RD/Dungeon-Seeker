import 'package:flutter/material.dart';

class DmScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("This is the DM Screen"),
    );
  }
}